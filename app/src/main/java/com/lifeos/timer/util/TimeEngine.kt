package com.lifeos.timer.util

object TimeEngine {
    fun formatDuration(ms: Long): String = com.example.util.TimeEngine.formatDuration(ms)
    fun formatTimestamp(timestampMs: Long): String = com.example.util.TimeEngine.formatTimestamp(timestampMs)
}
